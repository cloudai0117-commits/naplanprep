name: Deploy — UAT

on:
  push:
    branches: [develop]

env:
  REGISTRY: ghcr.io
  BACKEND_IMAGE: ghcr.io/${{ github.repository_owner }}/naplanprep-backend
  FRONTEND_IMAGE: ghcr.io/${{ github.repository_owner }}/naplanprep-frontend
  ADMIN_IMAGE: ghcr.io/${{ github.repository_owner }}/naplanprep-admin
  RAILWAY_API: https://backboard.railway.app/graphql/v2
  RAILWAY_PROJECT_ID: fc40e9ef-0983-4774-b3ec-7ec138bb370a
  RAILWAY_ENV_ID: 588e636a-0be4-4fee-8a7e-02ce26f5ec6f
  RAILWAY_BACKEND_SERVICE_ID: 58b84f41-861e-41e1-9440-e73d5a54e18b

jobs:
  build-and-push:
    name: Build & Push Docker Images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ steps.tag.outputs.tag }}

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image tag
        id: tag
        run: echo "tag=uat-$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & push backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:${{ steps.tag.outputs.tag }}
            ${{ env.BACKEND_IMAGE }}:uat-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          build-args: |
            VITE_API_URL=${{ vars.UAT_API_URL }}
            VITE_STRIPE_KEY=${{ secrets.STRIPE_TEST_PUBLISHABLE_KEY }}
          tags: |
            ${{ env.FRONTEND_IMAGE }}:${{ steps.tag.outputs.tag }}
            ${{ env.FRONTEND_IMAGE }}:uat-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push admin
        uses: docker/build-push-action@v5
        with:
          context: ./admin-panel
          push: true
          build-args: |
            VITE_API_URL=${{ vars.UAT_API_URL }}
          tags: |
            ${{ env.ADMIN_IMAGE }}:${{ steps.tag.outputs.tag }}
            ${{ env.ADMIN_IMAGE }}:uat-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy-backend:
    name: Deploy Backend to Railway
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Set Stripe env vars on Railway
        run: |
          railway_var() {
            curl -sf -X POST "${{ env.RAILWAY_API }}" \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer ${{ secrets.RAILWAY_TOKEN }}" \
              -d "{\"query\":\"mutation{variableUpsert(input:{projectId:\\\"${{ env.RAILWAY_PROJECT_ID }}\\\",environmentId:\\\"${{ env.RAILWAY_ENV_ID }}\\\",serviceId:\\\"${{ env.RAILWAY_BACKEND_SERVICE_ID }}\\\",name:\\\"$1\\\",value:\\\"$2\\\"})}\"}"\
              > /dev/null
          }
          railway_var "STRIPE_SECRET_KEY"    "${{ secrets.STRIPE_TEST_SECRET_KEY }}"
          railway_var "STRIPE_WEBHOOK_SECRET" "${{ secrets.STRIPE_TEST_WEBHOOK_SECRET }}"
          railway_var "STRIPE_PUBLISHABLE_KEY" "${{ secrets.STRIPE_TEST_PUBLISHABLE_KEY }}"

      - name: Update Railway service image
        run: |
          curl -sf -X POST "${{ env.RAILWAY_API }}" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.RAILWAY_TOKEN }}" \
            -d "{\"query\":\"mutation{serviceInstanceUpdate(serviceId:\\\"${{ env.RAILWAY_BACKEND_SERVICE_ID }}\\\",environmentId:\\\"${{ env.RAILWAY_ENV_ID }}\\\",input:{source:{image:\\\"${{ env.BACKEND_IMAGE }}:uat-latest\\\"}})}\"}"\

      - name: Trigger Railway redeployment
        run: |
          curl -sf -X POST "${{ env.RAILWAY_API }}" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.RAILWAY_TOKEN }}" \
            -d "{\"query\":\"mutation{environmentTriggersDeploy(input:{projectId:\\\"${{ env.RAILWAY_PROJECT_ID }}\\\",environmentId:\\\"${{ env.RAILWAY_ENV_ID }}\\\",serviceId:\\\"${{ env.RAILWAY_BACKEND_SERVICE_ID }}\\\"})}\"}"

      - name: Wait for Railway deployment
        run: |
          echo "Waiting 90s for Railway to pull and start the container..."
          sleep 90

      - name: Get Railway service domain
        id: domain
        run: |
          RESPONSE=$(curl -sf -X POST "${{ env.RAILWAY_API }}" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.RAILWAY_TOKEN }}" \
            -d '{"query":"{ deployments(input: { projectId: \"${{ env.RAILWAY_PROJECT_ID }}\", environmentId: \"${{ env.RAILWAY_ENV_ID }}\", serviceId: \"${{ env.RAILWAY_BACKEND_SERVICE_ID }}\" }) { edges { node { id status staticUrl } } } }"}')
          echo "deployment_response=$RESPONSE" >> $GITHUB_OUTPUT
          echo "$RESPONSE"

  deploy-frontend:
    name: Deploy Frontend to Vercel
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Deploy frontend to Vercel UAT
        run: |
          cd frontend
          vercel --token ${{ secrets.VERCEL_TOKEN }} \
            --build-env VITE_API_URL=${{ vars.UAT_API_URL }} \
            --build-env VITE_STRIPE_KEY=${{ secrets.STRIPE_TEST_PUBLISHABLE_KEY }} \
            --scope ${{ secrets.VERCEL_ORG_ID }} \
            --yes
        env:
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_FRONTEND_PROJECT_ID }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}

  deploy-admin:
    name: Deploy Admin to Vercel
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Deploy admin to Vercel UAT
        run: |
          cd admin-panel
          vercel --token ${{ secrets.VERCEL_TOKEN }} \
            --build-env VITE_API_URL=${{ vars.UAT_API_URL }} \
            --scope ${{ secrets.VERCEL_ORG_ID }} \
            --yes
        env:
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_ADMIN_PROJECT_ID }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}

  smoke-test:
    name: Smoke Test
    runs-on: ubuntu-latest
    needs: [deploy-backend, deploy-frontend, deploy-admin]
    if: ${{ vars.UAT_API_URL != '' }}

    steps:
      - name: Health check
        run: |
          sleep 20
          curl -f ${{ vars.UAT_API_URL }}/actuator/health || exit 1
          echo "UAT smoke test passed"
